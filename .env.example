# Optional — leave unset to run against the local mock backend (src/integrations/backend).
# Set both to switch the app to a real Supabase project (see src/integrations/backend/README.md).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
